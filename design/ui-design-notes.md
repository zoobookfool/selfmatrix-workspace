# UI 設計メモ(v1.5、2026-07-07)

**状態: UI 合意の正本。** Phase 2b (Cinny fork) で目指す画面の合意事項。
スパイク項目1の気づきから発展し、モックアップ2種(レイアウト案・ドッキングシェル)で
すり合わせて確定した。**仕様の正本はこの文書**であり、[mocks/ui-mock.html](mocks/ui-mock.html) は
操作できる視覚基準 (v2.2、ブラウザで直接開ける自己完結 HTML) として扱う。なお v2.2 は v1.4 時点の内容で、
v1.5 の変更 (リアクション UI 除外・話者ミニタイル抑制等) は未反映。衝突する場合はこの文書を優先し、
次のモック改訂で追随する。

v1.5 では 2026-07-07 の追加合意を反映する: カメラ送信を扱わない、リアクション系 UI は初期スコープから外す、
話者オーバーレイから音量調整へ到達できるようにする、別ウィンドウ通話開始モードの UI 合意を取り込む。

## 基本方針

- **配信特化・カメラ非対応**: 提供するのは音声 + 画面共有(配信)のみ。カメラは「UI を隠す」だけでなく、
  SelfMatrix の通常導線から取得・publish できない状態を目標にする。upstream 内部に dormant なカメラ実装が
  残ることは許容するが、ユーザーが気づかずカメラ送信してしまう経路は塞ぐ
- **配信の視聴はオプトイン**: 強制参加(自動再生)ではなく、見るかどうかを視聴者が選ぶ。
  見ていない配信は購読しない(帯域面でも合理的 — simulcast 購読自体を止める)
- **リアクション・効果音系は初期スコープ外**: Discord に相当機能はあるが、通話中の視界と音量安全性を優先し、
  SelfMatrix では非表示または無効化する。代わりにユーザー単位・配信単位の音量調整は必須機能として扱う

## 通話画面(element-call fork 側)

- **表示モードは注視⇔グリッドの 2 モード切替式**(v1.3 で 3 段構えから改訂):
  1. **注視(基本・既定)**: Discord 準拠。1配信を大きく表示 + 他はミニタイル。
     **ミニタイルをクリックすると注視対象がそのタイルに切り替わる**(v1.4)。
     ただし Cinny 埋め込みの通常画面では、視聴中の配信が 1 つでもある場合は話者ミニタイルを出さない。
     話者状態は配信上のオーバーレイで見せる
  2. **グリッド**: 視聴中の配信を**正方形に詰めて**均等表示(2本=1×2、3〜4本=2×2、
     5〜9本=3×3 …)。横一列に並べる方式ではない
- **視聴をオプトインした配信だけがタイルになる**: 未視聴の配信はタイルを作らず
  画面を分割しない。「視聴できる配信」の導線(チップ等)から視聴を開始する
- **グリッド中の「強調選択」(ON/OFF)**: ON にしてタイルを選ぶと、**選択したタイルだけで
  グリッドを組み直し(正方形詰めのまま)、選択しなかったものがミニタイルになる**。
  選択数は任意(**1 枚でも 3 枚以上でも可**)。OFF に戻せば均等グリッドへ。
  あくまで「グリッドの表示対象を絞る」操作であり、**モードはグリッドのまま**(v1.4)
- **強調選択で 1 枚だけ選んだときは見た目が注視モードと同じになるだけ**で、
  注視モードへの自動遷移はしない(v1.4 で改訂 — モード切替は常にユーザーの明示操作)
- **ミニタイルは必要な場面だけ実映像を流し続ける**(LiveKit simulcast 低解像度層を購読)。
  視聴中配信がある通常埋め込みでは話者ミニタイルを消し、別ウィンドウ通話など EC フッターを表示する
  standalone 寄りの画面では切替補助として使う余地を残す
- **ミニタイルの位置はユーザー選択可**: 上・下・左・右
- **話者表示は Discord StreamKit Overlay 風**: アバター+名前ピルを配信画面にオーバーレイ。
  発話中は緑リング+名前ハイライト、ミュートはマイクオフアイコン。
  **オーバーレイはドラッグで移動可能**(これのみ直接操作、他の配置設定は設定画面)。
  オーバーレイまたは話者ピルの右クリックから、そのユーザーのミュート・音量調整へ到達できるようにする
- **受信品質を見える化する**: 視聴中の配信には 720p 30 FPS などの品質バッジを表示し、カクつきが
  送信側・受信側・設定のどこに起因しそうか判断できる材料を出す
- **ポップアウトの主対象は「通話 UI 全体」**(v1.3 で再昇格): 通話 UI を丸ごと別ウィンドウへ
  出す。**目的は通話 UI とチャット UI を同時に見て・触ること**(ポップアウト中、メイン
  ウィンドウはチャット等の通常 UI に戻る)。実現方式は cinny 側スパイクで実証済み
  (widget の別ウィンドウ再ホスト、再接続 ~1.4 秒。popout-spike.md)
- 配信ストリーム単位のポップアウト(タイルの 🗗、再接続なし、実装済み)は補助機能として維持
- **配信の設定(画質と FPS)は配信者が選べる — Discord 準拠**(v1.4 追加):
  画面共有の開始時に解像度(720p/1080p/ソース解像度)と FPS(15/30/60)をピッカーで
  選択できる。既定はソース解像度/60fps。requirements.md §3 に対応要件あり
  (Sonnet レビュー M5 の「選択 UI なしは意図的差別化」判断はこの合意で上書き)
- **通話ステータス文言**: 画面共有が 0 本の通話を「ライブ配信中」とは表示しない。
  通話中は `通話中 {{count}}人`、配信中は配信者/配信数が分かる文言へ分ける
- **別ウィンドウ通話開始モード**: UI 合意は [call-window-mode.md](call-window-mode.md) v1.4 を参照。
  既定は別ウィンドウ、設定は二層保存、閉じる = 退出、別窓では EC 自身のフッター UI を表示する。
  ただし実装は [native-client-rethink.md](native-client-rethink.md) と [test-harness.md](test-harness.md)、
  および desktop window spike の最終 LiveKit join 検証待ちで保留

## シェル(cinny fork 側)

- **サーバー一覧枠・チャンネル一覧枠とも 上・下・左・右 から選択可**
  (上下配置時は横並び UI。チャンネル一覧はチップ列)
- **配置の設定 UI は設定画面に置く**(ドラッグハンドル方式にはしない)
- **設定画面などの PageNav は横チップ化しない**: サーバー/チャンネルリストの上下配置は main route の
  明示 opt-in に限定する。設定モーダルや管理系画面の縦ナビを壊さない
- **上下配置時の横チップ nav はカテゴリ内アイテムも横並び対応**: チャンネルリストを右/上/下に動かしても
  通話画面やチャンネル一覧が見切れないことをレイアウト要件に含める

## デフォルト(初見の友達が見る形)

- **配置セットアップの案内は SelfMatrix の初回利用時に 1 回だけ**(v1.3 で明確化)。
  まず Discord 風 / カスタムを選ばせ、カスタム時だけ詳細な配置を選ぶ。通話参加時やルーム参加時に
  毎回は出さない。あとから設定画面でいつでも変更できる旨を添える
- スキップ/未選択時のフォールバックは **Discord 風**: サーバー枠・チャンネル枠は左、
  ミニタイルは下、表示モードは注視(Discord と同じ挙動線)
- 別ウィンドウ通話開始モードの設定は初回セットアップ案内に含めない。通話設定で変更する

## 実装マッピング

| 対象 | fork | 対象コード(スパイクで特定済みの周辺) |
|---|---|---|
| 表示モード・注視選択・ミニタイル制御・話者オーバーレイ・カメラ送信導線の除去 | element-call | SpotlightTile / grid 系 UI(src/tile/, src/grid/) |
| 配信オプトイン視聴(購読制御) | element-call | LiveKit track subscription 制御 |
| メディアパラメータ(高画質化) | element-call | src/livekit/options.ts(スパイク項目2で実証済み) |
| サーバー枠・チャンネル枠のドッキング | cinny | ClientLayout 系。DOM 構造は upstream のまま CSS(flex-direction / order)+設定切替で実現 |
| 通話画面ポップアウト | cinny | CallEmbed(iframe widget)を別ウィンドウへ再ホスト。spike/call-popout で実証済み。残作業は製品実装・統合検証 |
| 配置設定の保存 | cinny | Matrix account data(端末間同期が効く) |

## リスク・注意

- ドッキング構想は fork-strategy.md「差分は小さく」と緊張関係 →
  レイアウト層の追加 + CSS 切替で実装し、upstream 追従の衝突面を最小化する
- EC のタイル UI は upstream でも活発に変わる箇所 → 新レイアウトは別コンポーネント追加で
  既存を温存する
- **方式は実証済み**(widget の別ウィンドウ再ホスト、E2EE セッションの扱いは spike/call-popout で確認済み)。
  本実装では再 join 中の表示・メインへ戻す導線・複数人通話時の見え方を重点検証する
- 初回セットアップの促しは「うるさくない」形にする(モーダル連発にしない)

## 改訂履歴

- 2026-07-07 v1.5: 追加合意を反映 —
  ①カメラは UI 非表示だけでなく通常導線から送信できないことを目標化
  ②リアクション/効果音系は初期スコープ外、音量調整を優先
  ③視聴中配信がある通常埋め込みでは話者ミニタイルを消し、話者オーバーレイへ寄せる
  ④話者オーバーレイ右クリックからユーザー音量へ到達
  ⑤別ウィンドウ通話開始モード v1.4 の UI 合意を取り込み、実装はネイティブ化検討待ちと明記
  ⑥初回セットアップは Discord 風 / カスタムの二択から始める

- 2026-07-06 v1.4: モック v2.1 への第 2 回フィードバックを反映(モック v2.2 と同期)—
  ①強調選択の意味を明確化: 選択したタイルだけでグリッドを組み直すだけで
  **モードはグリッドのまま**。1 枚〜3 枚以上まで選択数は任意。
  強調 1 枚 = 注視モードへ自動遷移、は**廃止**(見た目が注視相当になるだけ)
  ②注視モード中はミニタイルのクリックで注視対象を切替
  ③**配信の設定(画質と FPS)を配信者が選べる(Discord 準拠)** を新規合意
  (requirements.md §3 と roadmap Phase 8 バックログに追加)

- 2026-07-06 v1.3: モック v2.1 のレビューフィードバックを反映 —
  ①表示モードを「注視⇔グリッドの 2 モード + グリッド中の強調選択 ON/OFF」に再定義
  (旧「複数注視」独立モードを置換。強調 1 枚 = 注視へ、は従来どおり)
  ②グリッドは正方形詰め・視聴オプトイン外はタイル化しない(画面を分割しない)を明文化
  ③ポップアウトの主対象を「通話 UI 全体」に再昇格(目的 = 通話とチャットの同時利用)。
  配信単体ポップアウトは補助へ ④初回セットアップ案内は初回利用時 1 回のみと明確化。
  操作できる視覚基準として mocks/ui-mock.html を配置

- 2026-07-02 v1.2: ポップアウトの対象を「通話画面全体」から「配信ストリーム単位(Discord 方式、
  再接続なし)」に変更。技術検証で両方式の実現可能性を確認済み(popout-spike.md)。
  通話全体ポップアウトはオプション扱いに降格
- 2026-07-02 v1.1: モード体系を確定 — 注視が基本・既定(Discord準拠)、グリッドは追加オプション、
  複数注視(2面)はグリッドから発展。初版の「基本グリッド」「2面不採用」解釈を訂正
